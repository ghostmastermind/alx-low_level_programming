#include "main.h"

/**
 * _islower - Check description
 * @c: An input character
 * Description: Returns 1 if c is lowercase else eturns 0
 * Return: Nothing.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
